<h1>Add a Review</h1>
<%= simple_form_for [@restaurant, @review] do |r| %>
  <%= r.input :rating %>
  <%= r.input :content %>
  <%= r.submit class: "btn btn-success"%>
<% end %>
<%= link_to "Back to #{@restaurant.name}", restaurant_path(@restaurant) %>
